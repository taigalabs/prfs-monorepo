import { MarkdownWrapper } from "@/components/markdown/MarkdownComponents";

Shy users talk to each other with proofs that only they themselves can create.

In order to create a proof yourself, you need to have your data attested.

Check out <a href={props.attestationLink}>Prfs attestation</a> to start attesting your data.

export default function Layout({ children }) {
  return <MarkdownWrapper>{children}</MarkdownWrapper>;
}
