import React, { Suspense } from "react";

import styles from "./page.module.scss";
import { envs } from "@/envs";

const LocalPage = () => {
  return <div className={styles.wrapper}>local</div>;
};

export default LocalPage;
