# Verifiable computing

Verifiable computing is a set of technologies that aims to make verification of a computation
significantly cheaper than executing the computation. A prover, the one who **executes** the 
computation reveals the computation in a form of cryptographically treated number (s). 
