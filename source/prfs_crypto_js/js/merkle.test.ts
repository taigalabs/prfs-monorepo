import { computeRoot } from "./merkle";

describe("merkle", () => {
  it("should work", async () => {
    const siblings = [0];
    const pathIndices = [0];
    const leaf = 0;
    const root_ = 0;

    console.log(22);

    // const root = computeRoot(leaf, siblings, pathIndices);

    // assert()
  });
});
